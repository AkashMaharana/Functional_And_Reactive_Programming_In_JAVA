package com.akash.maharana;

public class ReducingOperators {
	
	//Take a series of emissions and reduce them to a single emission
	//works with finite observable only
	
	//count() : count the number of emissions in upstream
	
	//reduce() : identical to scan() but emits the final accumulation
	
	//contsins() : checks whether the specified emission present in the upstream or not
	
	//all() : verifies that each emission is satisfying the condition or not
	
	//any() : verifies whether any emission is satisfying the condition or not

}
